#!/usr/bin/env node
'use strict'

import calc from '../src/games/calc.js'

function _interopRequireDefault(obj) {
  if (obj && obj.__esModule) {
    return obj;
  }
  return { default: obj };
}

calc();
